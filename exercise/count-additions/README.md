A transformation that to each function adds a local variable which is incremented when an integer
addition is performed. The value of that function is then printed out before the function returns.
The code in `count_additions.cc` originates from the `InjectFuncCall.cpp` example from llvm-tutor.
